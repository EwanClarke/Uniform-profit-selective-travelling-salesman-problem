def simulated_annealing(adjacency_matrix, initial_temperature, cooling_rate, max_iterations):
    # generate an initial route, calculate its length
    # set the initial temperature and cooling rate
    # iterate for the max iterations or until a good enough solution is found
    #   generate neighbouring solutions
    #   select a neighbour and calculate its distance
    #   accept a neighbour based on a probability that decreases with temperature
    #   update the best solution
    # return the best route and its distance
    pass